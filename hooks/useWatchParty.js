/**
 * FICHIER MORT — la zone secrète (Watch Party) a été retirée du site.
 * Ce hook n'est plus importé nulle part : vous pouvez le supprimer.
 * (Le code WebRTC d'origine reste dans l'historique git si besoin.)
 */
export function useWatchParty() {
  return null
}
